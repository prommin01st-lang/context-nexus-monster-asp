meta {
  name: Sync Context File
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/Context/sync
  body: json
  auth: none
}

headers {
  x-api-key: {{apiKey}}
}

body:json {
  {
    "projectName": "{{projectName}}",
    "filePath": "{{filePath}}"
  }
}

vars:pre-request {
  baseUrl: http://localhost:5218
}
